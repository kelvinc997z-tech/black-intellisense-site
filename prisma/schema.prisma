generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Order {
  id              String   @id @default(cuid())
  targetAddress   String
  asset           String
  amount          String
  price           Float
  side            String   // "buy" | "sell"
  status          String   @default("pending") // "pending" | "approved" | "rejected"
  txHash          String?  // The transaction hash of the final step
  paymentHash     String?  // The transaction hash of the initial payment (Step 1)
  chainId         Int?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@index([targetAddress])
  @@index([status])
}
